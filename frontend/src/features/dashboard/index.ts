/**
 * Dashboard feature boundary (FRONTEND_CODING_STANDARDS §3): the authenticated home / overview
 * surface. Owns its own pages, components, and hooks; other features never import its internals.
 * Public surface only — empty until the dashboard slice is implemented.
 */
export {};
