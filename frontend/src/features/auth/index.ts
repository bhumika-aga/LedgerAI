/**
 * Auth feature boundary (FRONTEND_CODING_STANDARDS §3): sign-in, registration, session handling, and
 * route-protection wiring. Owns its own pages, components, hooks, and feature-local types; other
 * features never import its internals. Public surface only — this barrel is empty until the auth
 * slice is implemented.
 */
export {};
